import { attachElements, createElements } from "../app/create-elements";

const homepage = () => {
    let home = createElements("div", "On The Homepage");

    return home;
}

export default homepage;